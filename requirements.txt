requests==2.32.5
openai==2.41.1
jinja2==3.1.6
python-dotenv==1.1.0
beautifulsoup4==4.15.0
flask==3.1.3
flask-socketio==5.6.1
eventlet==0.35.0
markdown==3.10.2
nh3==0.3.6
weasyprint==61.0

# Multimedia/RAG context ingest extraction (osint_workbench/multimedia,
# osint_workbench/core/rag_ingest.py). All four degrade gracefully if
# missing -- extraction returns empty text with an error message instead
# of raising -- but without them, image/PDF/DOCX context uploads are a
# no-op; only .txt/.md work on stdlib alone. pytesseract additionally
# requires the `tesseract` CLI binary on the host system (not a pip
# package) -- install via your OS package manager, e.g. `apt install
# tesseract-ocr` / `brew install tesseract` / the Windows installer at
# https://github.com/UB-Mannheim/tesseract/wiki.
pillow==12.3.0
pytesseract==0.3.13
pypdf==6.16.1
python-docx==1.2.0

# Dev dependencies
hypothesis==6.103.1
